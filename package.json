{
  "name": "grok-video-generator",
  "version": "1.0.0",
  "description": "Grok Video Generator — Vercel deployment",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
